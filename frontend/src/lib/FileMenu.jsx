import { Menu } from "@mui/material"

const FileMenu = () => {
  return (
   <Menu 
     open={false}

    >
   <div style={{padding: "1rem" , width:"10rem" } } > 
   </div>
   </Menu>
  )
}

export default FileMenu